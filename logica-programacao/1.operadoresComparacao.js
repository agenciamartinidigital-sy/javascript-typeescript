/**
 * > maior
 * < menor
 * >= maior que
 * <= menor que
 * == igualdade (valor) não é recomendável
 * === igualdade estrita (valor e tipo)
 * != diferente - não é recomendável
 * !== diferente estrito (valor e tipo)
 */
const num1 = 10; // number
const num2 = '10'; // string
const comp = num1 !== num2;
console.log(comp);

